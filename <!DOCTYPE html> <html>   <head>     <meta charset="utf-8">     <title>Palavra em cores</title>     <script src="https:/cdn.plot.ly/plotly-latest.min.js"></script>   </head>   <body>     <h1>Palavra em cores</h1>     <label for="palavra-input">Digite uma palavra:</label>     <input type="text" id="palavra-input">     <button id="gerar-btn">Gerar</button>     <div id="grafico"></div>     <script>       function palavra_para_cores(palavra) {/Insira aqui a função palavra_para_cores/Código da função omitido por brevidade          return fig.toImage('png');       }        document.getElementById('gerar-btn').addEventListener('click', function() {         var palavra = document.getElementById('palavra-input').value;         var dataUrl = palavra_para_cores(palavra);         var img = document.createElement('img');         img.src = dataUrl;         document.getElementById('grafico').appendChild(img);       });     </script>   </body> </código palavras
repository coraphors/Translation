<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Palavra em cores</title>
    <script src="https://cdn.plot.ly/plotly-latest.min.js"></script>
  </head>
  <body>
    <h1>Palavra em cores</h1>
    <label for="palavra-input">Digite uma palavra:</label>
    <input type="text" id="palavra-input">
    <button id="gerar-btn">Gerar</button>
    <div id="grafico"></div>
    <script>
      function palavra_para_cores(palavra) {
        // Insira aqui a função palavra_para_cores

        // Código da função omitido por brevidade

        return fig.toImage('png');
      }

      document.getElementById('gerar-btn').addEventListener('click', function() {
        var palavra = document.getElementById('palavra-input').value;
        var dataUrl = palavra_para_cores(palavra);
        var img = document.createElement('img');
        img.src = dataUrl;
        document.getElementById('grafico').appendChild(img);
      });
    </script>
  </body>
</html>
